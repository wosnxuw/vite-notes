# 生态

这篇文章记录大模型落地后，整个外围应用的管理，比如 Agent Workflow 上下文管理 等等

### 沙箱化趋势

Codex 会抢占你的终端，他改的时候你没办法操作，所以目前一个趋势是上云，上沙箱。给它分配几个干净的小型环境，来专门干活

沙箱这个东西你还可以在云服务商那里买到，类似 FaaS，你安装 SDK 后，一行函数，就可构造沙箱，一行函数，就可让沙箱运行 python 脚本

我看 manus 官网介绍，说他们会用沙箱技术，部署多个子 Agent，独立并行测试

### 训练框架与推理引擎

PyTorch、TensorFlow 训练框架 这个不多说了好吧

TensorRT、ONNX Runtime 通用推理引擎，它是你先在 PyTorch 里训练好了，然后部署到这里，用于生产级别

TensorRT 是 N 卡专用的推理优化引擎，注重极限优化

ONNX Runtime 是微软开发的一个通用引擎，CPU GPU，各种操作系统都能跑，格式为 onnx

vLLM、llama.cpp 大语言模型？

通用引擎它什么架构都能跑，包括 CNN，RNN，Transformer，但是这俩只能跑 Transformer

vLLM 提出了一种叫做 PagedAttention 技术，高效的管理 KV 缓存，一般企业会用。它是借助于 pytorch 或者 ONNX Runtime 来跑，相当于一个框架，而不是引擎（这个我也没用过，不知道具体）

llama.cpp 这个确实是一个引擎，它专注在资源受限设备上跑 LLM，gguf 格式

然后我想到 NVIDIA 的推理卡、训练卡和游戏卡，这东西应该属于是商业上的划分，没有绝对限制

你的笔记本不是一样能本地训练一个 CNN 推理嘛，但是如果你视图去微调 LLM，那不行，因为一个是显存小，再一个是部分卡（推理卡）的 FP32 模块被阉割，只是显存傻大

所以这个 pytorch 这种训练框架，它专注于 前向、反向传播，损失计算，梯度更新等整个流程

而 TensorRT 一般不训练，只推理（有的也能微调，但是不能完整练）

### Skills

现在这个 Claude 把 Skills 的标准公开了，很多工具现在也支持

https://code.claude.com/docs/en/skills

元数据：必定加载（目录），用于降低工具太多导致的 Token 消耗

指令：正文

资源：附录

每个 skill 是一个文件夹（对比 mcp，它是一个可执行程序），包含一个 SKILL.md，此 md 包含了 元数据和指令

然后罗列一大堆子文件夹，每个子文件夹都是资源，资源可以是图片，文本，也可以是脚本

元数据就是 Markdown 的元数据，和我用 hugo 的时候必须填的那个类似，一个 yaml

```yaml
---
name: 复制 XML
discription: 把 runtime 下面的 XML 备份起来
---
```

指令部分其实就是写一个特别详细的提示词，来提示 AI 如何操作。如果你不需要脚本，那么就不需要子文件夹的资源

注意，每次你提问，Claude 会扫描你所有的 SKILL 的元数据，就和工具类似，你只要开启，就直接加入上下文，所以你不用，就不要开

一句话：Skills 是一种提示词管理模式，相当于大号提示词数据库，渐进式的加载提示词。MCP 是一个工具箱，你不要拿太多

这东西我感觉有点傻，但是可能确实有用吧。我认为直接抄别人的 SKILL 是比较好的，不要自己写，但是门槛比较低，这东西执行你 python 的时候，我估计错误处理啥的也是个问题。精准还得是 MCP，但是专用 MCP 非程序员不好写，我看 Cherry Studio 里也提供现成的 MCP

### Manus

[2026-1-30]

最近，我需要做年终总结 PPT。虽然我知道很多 AI 都能做，但是我考虑到付费以及本身我没怎么用过写 PPT 的功能。

我就萌生了一个点子：我写 markdown，让 AI 总结为符合 PPT 的 markdown，再用前端相关工具转换为 PPT 或者网站。我一问 Gemini，果然有类似工具，于是我选用的 Slidev。

然后我发现 Slidev 它官网提供一个 Skills，我把那个 Skills 拿到仓库里，搭配 OpenCode 使用，模型是 ds，写了一会，一次就跑起来了，然而只是内容可以，动画/界面效果几乎没有。虽然加载了 skills，可能 ds 遵循指令的能力不行，或者是本身 slidev 就小众，反正还不如用那些现成的服务。

[2026-3-3]

原来有公司意识到这一点。MiniMax 的 Agent 模式似乎就是可以写 slidev 的 PPT。

### n8n

[2026-1-19]

这东西去年就开始火，但是我一直没用，今天试了一下

感觉必须是很复杂的项目才需要用这个东西

这东西号称是 no-code，但是实际上，所有的东西都是微服务设计，架构分离，该少的东西你一个不能少（比如你要搭 RAG，那么你一样要部署 Milvus），但是灵活性是够的

这东西应该说是串联起最后的流程，方便产品经理进行调整、调试

比如你做 RAG 产品，你最终的效果好还是不好，那取决于你的 文档解析、数据分块，实际上和 n8n 没啥关系

### Dify

[2026-1-26]

去年早期试过 Dify，但是觉得效果很屎，但是那个时候我确实也是刚接触 RAG

经过一年的摸爬滚打，RAG 在行业内甚至被认为是已死的技术

反正今天又试了一下，感觉还行吧，也有工作流设计，但是没有 n8n 那么多

不过 Dify 现在支持多模态嵌入模型，以及自动将图片绑定到文本附近，提问时，可以输入给视觉模型，这样效果比传统 RAG 更好

### OpenCode

[2026-1-26]

之前试过 QwenCode，但是它只局限于 Qwen，今天试了一下 OpenCode，基本上和 QwenCode 类似，我调的 OpenAI 的模型，跑了个小 Demo

感觉 OpenCode 的意义不是特别大？和我用 Zed 编辑器直接唤起 AI 对话框，然后 AI 就能直接操作了

我看网上有人说 OpenCode 很好用，他用了很久，QwenCode 是最垃圾的，ClaudeCode 最强

我感觉 OpenCode 似乎有一个 linux 命令解释器，反正 AI 运行命令的问题很小，要是 VSCode 去弄，经常 AI 命令不是 Windows 的

[2026-1-30]

我对比了一下这些工具，我分为四类

（1）VSCode 本身+各种插件

各种插件毕竟是第三方的，可能结合不是那么美好（其实就是内嵌一个网页，高分辨率下，很多字体大小都不统一）

（2）VSCode 分叉版

Cursor 刚火的时候试过。我看 Cursor 应该是有点黑科技的，尤其是读他们的博客。应该是内置了很编译器数据库，据说是先跑小模型理解你的意愿，再找内容，输入给 AI 要看的内容很准，也省 Token。
是的，你想，Cursor 本身也是调 Claude（抛开价格优势），那为什么不买 API 再用 VSCode 插件呢？说明还是有东西

Trae 半年前试过

Antigravity Google 出的，目标是多 Agent 同时协作。我没下载。我看 reddit 上的评价不是特别好？目标群体是那种顶级 AI Boy，依靠十几个 AI 协作，每天上万行代码，甚至无法人工 review 的极客

（4）Zed

应该是没有 Cursor 那种黑科技的，但是 AI 确实内置到了侧栏。和 IDE 内置的类似，也是通过查看文件的方式（虽然可以读取一部分行，但是我看 AI 很多时候倾向于一直看，看到结束）来工作

（5）命令行

比如 Claude Code、Codex、Qwen Code、Gemini CLI、Open Code

这类工具的思路是，给 AI 提供 MCP 工具，让 AI 真正的像人类一样，先 ls 查看项目结构，然后再 grep 查看关键点

总结：受限于训练成本，大模型本身确实是 AI 时代的护城河。但是外围生态有点隐约成为城墙的趋势。现在一看模型答题评分都巨高，但是实际体验确实是有差距。

### Browser-Use

[2026-1-26]

Gemini 推荐我尝试的玩具，类似 OpenAI Operator，Anthropic Computer Use

这个项目的文档还不错（也许是需要写的代码很少）

我尝试了一下，配置的模型是 qwen-vl-max

外观上看起来像是一个超级大型的提示词工程

(1)
任务是 "访问 https://t.bilibili.com 然后列出谁正在直播。"

我的意图是用我真实的浏览器 通过 JWT 让它能访问我自己的动态页，在左边观察谁在直播

执行了一下，它是启动了浏览器，也能点击，但是没有我的 JWT，然后它尝试用 live.bilibili.com 找，把这个界面打开了好几次，最后失败了

检查了一下似乎路径的位置是对的，但是不知道为什么不行

(2)
任务是 "访问 Github 上 wosnxuw/vite-notes 这个仓库，检查最后一次的 Actions 是否成功"

因为这个事情不需要登录，它成功了

可以看到是有任务规划的，并且会整理为 Memory 避免上下文过长

```
INFO     [Agent] 🎯 Task: 访问 Github 上 wosnxuw/vite-notes 这个仓库，检查最后一次的 Actions 是否成功
INFO     [Agent] 📍 Step 1:
INFO     [Agent]   🎯 Next goal: Navigate to the GitHub repository page for wosnxuw/vite-notes.
INFO     [Agent] 📍 Step 2:
INFO     [Agent]   🧠 Memory: Visited the GitHub repository page for wosnxuw/vite-notes. The repository is public, and the latest commit was made 2 hours ago. The next step is to check the Actions tab to determine the status of the latest GitHub Actions workflow.
INFO     [Agent]   🎯 Next goal: Click on the 'Actions' tab to view the GitHub Actions workflows for this repository.
INFO     [Agent]   ▶️   click: index: 1614
INFO     [tools] 🖱️ Clicked a "Actions" id=actions-tab

📄  Final Result:
已访问 GitHub 仓库 wosnxuw/vite-notes，并检查了最新的 Actions 状态。最新的一次运行（Run #11）成功完成，状态为 'Success'，总耗时 38 秒。该运行包含 
两个作业：build（21 秒）和 Deploy（9 秒），均成功执行。部署的网站地址为 https://wosnxuw.github.io/vite-notes/。
```

嗯，我看了下，OpenAI 和 Anthropic 在工作流这一块东西还挺多的

### Claude Code

[2026-3-3]

其实很早就下载了这玩意，毕竟大名鼎鼎了算是。但是因为没订阅 Claude，所以随便给了个 ds 就扔了，一直没怎么仔细的用起来。

最近用了一下，我发现它确实有说法。它的提示词系统更加精细、完备。即使同一个模型，换到 Claude Code 上也会表现更好，超过 OpenCode。

根据我的一些了解，做这种 CLI 工具，它的思想其实就是 Bash 操纵一切。

一个最简单的 CC 如下，一个 python 文件，就给模型仅仅一个工具，那就是执行命令，然后返回命令的执行结果

子 Agent 不需要，因为通过在 bash 里重新执行 python cc.py 即可唤起一个子 agent 工作，然后获取其结果

需要看就 cat，需要写就 echo 之类的

然后里面搞一个死循环，一直调 Agent 工作即可

**重点：**

其实我最近发现上下文管理（提示词系统很有用），如果把大模型比作 y = f(x)，你想要更好的 y，那么确实 x 需要更好

CC 的提示词大概有几个点：

（1）描述何时使用子智能体：读取大量文件，任务是独立的，避免细节污染当前对话

（2）不要做多余的事情

而且 CC 是会做项目的记忆的，把重点记下来，以便于后续查找，而且是分层的。

Claude.md 用于总体管理，并且每个子目录都可以放子的。

而且对话中，还会创建任务清单，通过打勾的方式确认做了什么。

CC 还会像人一样，搜索代码，实时阅读，并不需要把所有东西放在上下文里。修改也是按行修改，省上下文。

只能说它确实有效果。

CC 即使我选择 Sonnet 似乎也会用 Haiku 规划一些东西。

### OpenClaw

[2026-3-3]

部署完了，不知道用来干嘛。

### Claude Blog

https://claude.com/blog/best-practices-for-prompt-engineering

按照输入输出理论，确实应该提高输入的质量了

总结下这篇文章的亮点：

（1）直接说明你想做什么。

使用简单的语言，明确表达你的需求，且不含糊。不要绕弯，不要模糊。

什么使提示足够具体？
明确约束（字数、格式、时间线）
相关语境（受众是谁，目标是什么）
理想的产出结构（表、列表、段落）
任何要求或限制（饮食需求、预算限制、技术限制）

（2）提供做事的动机。

不要：不要使用要点

要：我更喜欢以自然段落的形式回应，而不是以要点来表达，因为我觉得流畅的散文更容易阅读，也更善于交谈

（3）告诉人工智能该做什么，而不是不该做什么。

不要：不要在回复中使用“markdown”。

要：你的回复应由流畅流畅的散文段落组成

（4）将你的提示样式与期望的输出相匹配。

如果你的输入标记很多，那么他也会这样

（5）使用 XML 来提高清晰度似乎没那么大用

（6）你是数学领域专家。基本没用，过于具体的角色可能会限制人工智能的帮助。